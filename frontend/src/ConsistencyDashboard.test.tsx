import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,expect,it,vi} from 'vitest';import App from './App';afterEach(()=>cleanup());
it('shows text and icons for fail, not verifiable and LLM suggestions',async()=>{vi.stubGlobal('fetch',vi.fn((input:string|URL)=>{const url=String(input);const body=url.includes('/dashboard')?{status_counts:{PASS:1,WARNING:0,FAIL:1,NOT_VERIFIABLE:2},unresolved_high_critical:1,qdrant_orphan_vectors:'NOT_VERIFIABLE',missing_vectors:'NOT_VERIFIABLE',unsupported_agent_answers:1,untraced_requirements:0,blocked_models:1,reports_requiring_regeneration:0}:[{finding_id:'f1',status:'FAIL',rule_id:'CON-RAG-001',message:'citation 오류',category:'RAG_EVIDENCE',severity:'HIGH',validation_type:'LLM_SUGGESTED_FINDING',automatic_action:'ROUTE_TO_REVIEW',assigned_role:'QA_RA',resolved:false}];return Promise.resolve(new Response(JSON.stringify(body),{status:200,headers:{'Content-Type':'application/json'}}))}) as any);render(<App/>);fireEvent.click(screen.getByRole('button',{name:'통합 정합성 검증'}));expect(await screen.findByText(/✕ FAIL · CON-RAG-001/)).toBeInTheDocument();expect(screen.getAllByText('NOT_VERIFIABLE').length).toBeGreaterThan(0);expect(screen.getAllByText(/LLM 제안/).length).toBeGreaterThan(0)});
