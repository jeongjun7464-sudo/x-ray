# Sample data

이 디렉터리에는 합성 또는 적법하게 공개된 비식별 영상만 두십시오. 의료영상은 Git에 커밋되지 않습니다. 폴더 클래스 방식과 `file_path, anatomical_region, laterality, view_position, patient_group_id, institution_id` 컬럼의 CSV manifest 방식을 지원합니다.
