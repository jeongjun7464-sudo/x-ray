# 반복 오류·CAPA 워크플로

QA_RA 또는 ADMIN이 오류 발생을 등록한다. 동일 오류 유형이 설정 횟수 이상 누적되면 모델 버전, 데이터셋 버전과 익명 분석 ID를 연결한 `CANDIDATE`를 생성한다.

근본원인, 시정·예방조치, 담당자, 기한, 효과성 확인을 기록하고 승인자 없이 `APPROVED` 또는 `CLOSED`로 전환할 수 없다. 자동 생성은 조사 후보일 뿐 결함 원인을 확정하지 않는다.
